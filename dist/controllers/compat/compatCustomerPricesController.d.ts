import type { Request, Response } from 'express';
/**
 * Per-customer Service (article) pricing in AdminSettings JSON.
 */
export declare function handleCustomerPrices(req: Request, res: Response): Promise<void>;
